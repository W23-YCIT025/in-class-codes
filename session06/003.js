const animals = ['ant', 'bison', 'camel', 'duck', 'elephant'];
const cats = {
    name: 'cat1'
};

console.log(`Type of animals: is ${typeof animals}, Is Animals array ${Array.isArray(animals)}`);
console.log(`Type of cats: is ${typeof cats}, Is cats array ${Array.isArray(cats)}`);